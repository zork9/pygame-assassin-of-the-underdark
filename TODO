. for graphics on screen : in game.py line : self.room.draw(self) must be images blit in while loop of SDL events (where pygame display update is) 

. widget system needs mainloop class of func
- grep for FIX
. pickup enemies with 'x' key is possible but not yet implemented
	you need Goblin.inventoryitem for it for example
. remove sleep fnuctions
. use of item functions
. picking up gameobjects/enemies
. make interface for addpicture etc in statimagelibrary and stateimageresourcelibrary
. spells are inventoryitem deriveds, change to own base class
